{
  "name": "fwa",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "local-pack": "turbo run local-pack --force",
    "playground": "node ./scripts/playground.js",
    "watch": "turbo run watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "fs-extra": "^10.1.0",
    "turbo": "^1.3.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
