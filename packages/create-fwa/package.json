{
  "name": "create-fwa",
  "version": "0.1.0",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "commander": "^9.3.0",
    "fs-extra": "^10.1.0"
  }
}
